<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Privacy Whitelist & Phonebook lookup -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    
    <!-- Floating Protective Threat Shield Window -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    
    <!-- Foreground Service for real-time background protection -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RakshakAI"
        tools:targetApi="31">

        <!-- Main Dashboard & Setup Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Full-screen Micro-Friction Gate Activity before UPI Debit -->
        <activity
            android:name=".ui.MicroFrictionActivity"
            android:exported="false"
            android:theme="@style/Theme.RakshakAI.Dialog" />

        <!-- Real Android Notification Listener Service for WhatsApp & SMS -->
        <service
            android:name=".service.RakshakNotificationService"
            android:label="@string/notification_service_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Floating Heads-Up Shield Overlay Service -->
        <service
            android:name=".overlay.FloatingAlertOverlayService"
            android:exported="false" />

    </application>

</manifest>
